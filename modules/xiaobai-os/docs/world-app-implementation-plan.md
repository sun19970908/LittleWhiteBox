# 普通小白 OS 世界 APP：施工与验收记录

## 1. 当前决定与范围

记录日期：2026-09-06。产品定位见 [世界 APP 设计](./world-app-target-design.md)。

原生 ↑Char 加载事件的请求隔离验证失败后，用户明确改为 **System @D4**，并要求继续施工。本版已接入世界 APP、桌面图标与 D4 短背景；没有把失败的世界书临时条目方案留作运行分支。历史证据见 [↑Char 验证记录](./world-app-native-injection-verification.md)。

世界是镜头外的轻背景阅读 APP，不是任务、剧情导演或百科。首版后用户确认：将已保存的世界新闻直接加入任务、地图现有素材包，本次已接通。信息联动、ENA、小白板迁移与退场不在本轮范围。不自动提交或推送。

### 1.1 已确认行为

- 玩家订阅并阅读当前新闻；作者设定约束创作，新闻不必等玩家亲历才出现。
- 维护同一组见闻：延续、保持、退出与加入均可，不要求每轮制造新事件。
- 主剧情只收到概况与独立摘要；文章正文供玩家阅读，不等同于角色已经知情。
- 不建设人工纠正环境、历史档案、逐条编辑、点赞或已读系统。

### 1.2 首版运行参数

| 项目 | 已采用规则 |
| --- | --- |
| 初始偏好 | 未订阅；「作为剧情背景」开启 |
| 订阅 | 保存意愿后申请首批；之后每个有效接受轮参与维护，可能使用模型但内容不变 |
| 无开场正文 | 保存订阅意愿，等待开场；打开页面本身不生成 |
| 取消订阅 | 停止自动维护并使未提交的所属运行失效；保留新闻和背景偏好 |
| 手动刷新 | 无需已订阅，沿用当前新闻 |
| 容量 | 最多 8 条；ID／标题／摘要／正文／概况分别最多 64／64／120／800／320 Unicode 码点 |
| 投影预算 | 初始资料安全格式化后不超过 16,000 码点，超出时省略正文并说明；主剧情短背景含说明不超过 2,000 码点，超出时选取完整概况／摘要，不影响原文保存 |
| 历史分支 | 保留订阅和背景偏好，清空概况与新闻，在子线重新建立 |
| 完整复制、改名 | 保留当前内容，后续各聊天独立维护 |

## 2. 所有权与终态结构

```text
domains/world/              当前模型、字段校验、原子编辑、纯数据读视图
apps/world/
  descriptor / partition / module / production-module
  prompt-data               后台资料包装与注入预算
  application/              分区服务、内容并发比较与偏好保存
  maintenance/              领域 Prompt、工具契约、运行内候选
  host/                     控制器、维护注册、D4 投影与生命周期、分支处理
  ui/                       刊物首页、独立文章页、本地图标与样式
```

- 唯一事实来源：当前聊天 sidecar 的 `world` 分区。卡片、激活世界书、近期剧情及既有有界总结为只读背景，不再建世界书副本。
- 持久态：用户两个偏好和当前概况／新闻。它们需要跨重启恢复；分区服务拥有写入和删除生命周期。
- 临时态：候选、未解决的工具错误、请求 token、注入快照、文章快照、滚动与焦点。不新增持久化版本号、运行日志、缓存或世界时钟。
- 外部依赖：既有 ScopedChatStore／文件恢复、maintenance／Provider、宿主生成生命周期与浏览器 SVG／CSS；无新增运行依赖。
- 注册：Host／Shell catalog、Shell 懒加载、生产组合、World module 的分区／维护／Prompt 接口；可选资料通过 World 自有只读 context capability 提供。核心维护层不识别 World 业务。
- 删除：先停止并注销 World 运行和监听，清除 `world` 分区及其注入键，再删功能目录、注册、任务与维护素材包中的只读接入，重建产物。无需修改聊天正文、作者世界书或其他 APP 数据。
- 兼容：依赖现行 SillyTavern、浏览器、Provider 与 sidecar 协议。未发布 World 没有旧模型迁移或永久兼容分支；不迁移正式线小白板数据。

## 3. 数据、工具与保存边界

持久化格式为 `{version:1,subscribed,injectToStory,overview,news}`。每条新闻仅含 `{id,title,summary,body}`。未知字段、重复 ID、空标题／摘要／正文和超限字段在领域入口拒绝，不自动截断或丢条目。存档合法性不依赖注入说明文字或安全转义后的长度；领域不依赖提示词格式化。

模型初始数据与 `WorldRead` 共用 `{overview,news}` 投影，不外露偏好、存储版本或宿主对象。初始资料通常完整；安全格式化后超过 16,000 码点时，仅在输出副本中将所有正文置空，并明确说明正文省略、其余字段完整。当前字段上限保证摘要视图在最坏转义情况下也能放下。`WorldRead` 参数为空，始终返回完整投影，可补读省略的正文或核对编辑后的候选，不返回 Edit 的状态包装。工具读取不受初始资料预算截断。

`WorldEdit` 接收可选 `overview`、完整条目 `upsert`、ID 列表 `remove`：

- 省略概况保留，空字符串清空；省略新闻原样保留。没有 null 或局部新闻合并语义。
- 同一 ID 延续原条目，原条目保留顺序；新条目按输入次序置于前面。
- 删除不存在的 ID 是成功但无变化；同批重复或增删冲突拒绝。
- 整批校验后才替换候选；任何错误不产生部分删除、部分概况或残缺文章。
- 返回 `{ok,status,changed,data:{overview,news},errors:[{path,message}]}`；`status` 为 updated／unchanged／failed。unchanged 是成功，不要求重试。
- 失败批次按受影响概况／文章记录运行内未解决项。后续无关编辑不能抹掉失败；成功调用的 errors 仍说明此前待修正项，解决后才能提交。无法识别有效条目的坏调用可用合法的具体编辑纠正。
- 放弃失败的新稿可 `remove` 其 ID；已有文章的 `remove` 是真实删除。若只放弃对已有文章的修改，先从 `WorldRead` 取原值再完整 `upsert`；概况同样可重交原值。合法的 unchanged 修正会解除对应错误，不要求制造新变化。

提交比较原 `{overview,news}`、聊天身份和维护 guard，不以另一 APP 的写入作为正文冲突。正文提交保留事务内最新偏好，无额外持久化 revision。

只有确认保存的内容进入界面和注入。World 声明 `complete-run` 提交策略：工具循环完整结束且本领域没有未解决错误后，才发布整期候选。网络中断、空返回、轮数耗尽或未解决的工具错误均保留旧刊物，不保存中途删除或替换的结果。共享执行器不认识领域名；未声明此策略的地图等领域继续保留合法的部分结果。

保存失败保留旧内容；未确认的候选交给现有文件恢复，UI 提供「核实保存」，不会再生成一份覆盖它。

## 4. Prompt 分层与内容标准

实现遵循 `writing-model-prompts`，原型为助手的 `system-prompt.js` 与 `tooling.js`。

| 层 | 归属与事实依据 |
| --- | --- |
| 共享说明 | `capabilities/maintenance/provider-tool-loop.ts`：身份、背景资料、跨领域流程和唯一收尾，World 不重复 |
| 领域判断 | `apps/world/maintenance/prompt.ts`：镜头外新闻、具体看点、依时间延续、公开消息与传闻／私密知识边界 |
| 工具契约 | `apps/world/maintenance/tool-contract.ts`、`domains/world/edit.ts`：原子批次、返回形状、顺序、失败修正和字段语义 |
| 初始资料 | `maintenance/session.ts`、`apps/world/prompt-data.ts`：复用纯数据投影，包装完整或省略正文的资料说明并控制预算 |
| 主剧情资料 | `apps/world/host/story-projection.ts`：已确认概况与摘要、镜头外背景定位，安全格式化并选择放得下的完整段落 |

模型说明默认使用英文，新闻语言随当前故事。主剧情背景说明也保留英文，不因相邻块使用中文而额外改写。无运行提示词写入持久化文档。领域块点名实际资料标签与缺少正文时的读取入口；首刊以资料能否支持符合场所、时代或生活方式的具体镜头外事件为判据。工具保留字段限额，不要求模型计算转义后长度；共享层按各工具实际应用／拒绝结果指导修正，不暗示失败原子批次会保留部分修改。

真实生成评审看：是否有具体而独立的看点、是否体现世界的文化和生活、是否不依赖玩家参与、是否依据经过的时间延续、传闻和事实是否保持各自确定性。轻背景不等于每条鸡毛蒜皮，也不等于每条危机。明确剧情能纠正旧背景；私人内容不会自动变成公开报道。

这些是产品质量标准，不另加评分 Agent、喜好画像或自动评分存档。手写测试数据不证明模型能写好。

## 5. 订阅与维护生命周期

自动维护沿用现有有效接受轮；只有已确认订阅的当前聊天参与。首次订阅和手动刷新使用 `startRebuild('world')` 的有界历史输入，但 World 候选仍从旧内容开始，不清空重建。

首次订阅先检查 Agent 与文件状态，保存偏好后才启动。空聊天保存意愿后等待；API 配置不足给出 API 应用入口。打开、阅读、返回、重进只读本地。

首次订阅保存未确认时不请求模型；用户核实成功、已确认订阅由关闭变为开启后，接着启动首批更新。只比较本次核实前后的订阅状态，不保存额外的续跑标记；重复核实、新闻或背景开关的保存核实不触发生成，核实期间切聊天或离开页面也不续跑。

取消订阅同时使 requested／automatic token 失效，重新订阅不会复活旧请求。切聊天、关闭 OS 窗口、停用 OS 均使未提交运行失效。仅切换 APP 不取消更新。关窗不取消长期订阅，后续有效接受轮仍可维护。取消只针对 World，不停止队列里其他领域。

历史分支在 clone 边界清新闻，父线不受影响。`host/production-composition.ts` 分别组合 Messages 和 World 自有分支处理器，不把 World 逻辑放进 Messages。

## 6. System @D4 接入

`host/sillytavern-runtime-adapters.ts` 的现有 `setSillyTavernPrompt` 增加可选 depth，默认仍为 1。World 明确使用 4；Map 按后续确认的空间背景定位明确使用 3，Tasks／Shop 默认 D1 不变。

- 原生位置为 IN_CHAT、SYSTEM、depth 4，`scan=false`。不再注册世界书加载监听，不读取或写入作者条目，也不受 ↑Char 区块开关和世界书条目预算支配；最终请求仍遵守宿主的上下文组装。
- 本次主生成拦截时，从当前聊天已确认内容取一次快照。维护后到的新内容从后续请求生效，不等待维护。
- 支持 normal／regenerate／swipe／continue；quiet、impersonate、dry-run 不经 World 注入入口。
- 请求已组装、结束、取消、切聊天或停用时清除所属注入键。关闭背景也清除待组装投影。
- 只投概况与摘要，不带正文、内部 ID 或偏好。文本经过安全格式化；宿主宏与标签不从新闻内容逃逸执行。
- 超过 2,000 码点时先计入说明开销，再按原顺序选择放得下的完整概况／摘要，标明有省略。过长概况可跳过，继续选择摘要；一段也放不下则不注入。不截半句话或转义序列，不更改存档和阅读内容。

### 6.1 真实宿主验证证据

在独立数据目录启动 SillyTavern 1.18，未读取或修改用户聊天，未请求 Provider：

- 实际运行时适配器设置 SYSTEM／IN_CHAT／depth 4／scan false，原 D1 对照不变。
- 原生 `prepareOpenAIMessages` 组装结果中，World 后恰有四条历史消息。
- 文本补全 `Generate(..., dryRun=true)` 搭配固定 D4 扩展提示，也保留四条历史消息；此探针证明宿主文本组装位置，不冒充生产 dry-run 会注入 World。
- 主扫描与后台 dry-run 扫描交错时，World 文本没有激活关键词测试条目；实际 OS PromptContext 捕获也未混入新闻。
- 覆盖 normal／regenerate／swipe／continue 的生命周期清理、取消、dry-run／quiet 排除、切聊天和卸载。

聊天补全／适配器探针有 21 条实际断言；另有文本补全组装探针。脚本与隔离数据位于忽略目录 `output/playwright/world-native-gate/`，不随产品打包。

尚未覆盖真实群聊完整生成、用户复杂预设组合与 WebView；不将这些标为已通过。

## 7. 世界窗口 UI

用户确认替换初版复古小刊风格。继承 OS 字体，近白底、墨色正文与浅蓝远景，深色中性炭黑/柔白与夜色。主标题 22px、新闻标题 16px、摘要 13px，保留触控热区，不同时放大所有元素。本地 SVG 仅是抽象品牌远景，不代表世界内真实地貌；没有远程字体、素材服务、模型配图或新依赖。

首页只有一个「世界」标题；开篇图与概况分离，概况最多预览两行，主动展开才看完整内容。下方直接排列同级新闻，不再加栏目眉题、计数和逐条箭头；标题与摘要分别最多预览两行。内容区宽度达到 640px 时两列，其余单列，不改 Shell 窗口尺寸。删除首篇特大排版、编号、印章与口号。文章页展示完整标题和纯文本分段正文，不重复摘要、不执行 HTML。

空态主操作为「订阅新闻」，说明会使用模型。已有内容后顶部可刷新，订阅和背景开关进入右上菜单。更新中旧文章可读；失败就地提示，提供重试或保存核实，不反复弹通知。

阅读文章持有临时快照；后台修改时提供「阅读新版」，删除后仍能读完当前快照。返回恢复原列表滚动和条目焦点，条目已移除时聚焦首页标题；打开或显式读取新版后聚焦文章标题。菜单支持 Escape 和点击外部关闭。没有虚构发布时间、未读红点、热搜或往期档案。

所有权：`WorldApp.vue` 编排页面与现有操作，`WorldOpening.vue` 只负责视觉开篇和概况展开，`NewsArticle.vue` 负责正文阅读；SVG 与样式留在 `apps/world/ui/`。唯一内容来源仍是现有确认状态，展开状态和阅读快照不持久化。移除世界 APP 时随目录退场，没有共享层新注册、新字段或迁移。

## 8. 验收状态

### 8.1 已执行的自动测试

`world-publication.test.js` 与 `world-runtime.test.js` 使用真实领域、分区事务、服务和维护链路，仅隔离外部存储／Provider：

- 创建、延续、保持、删除、顺序、完整读回；原子错误与单项纠正，非法 ID 可纠正。
- 码点限额、最坏转义数据经过工具／存储／重开不变、初始资料超预算省略正文与 Read 补读、主剧情预算只保留完整安全段落。
- 偏好／正文并发、失败／未确认／同一候选恢复、失效 guard 与分支规则。
- 首次订阅、空聊天、配置不足、取消后重订阅、切聊预检竞态、自动／手动运行、切换 APP、关闭窗口与停用 OS。
- 确认快照、背景开关和 Prompt 生命周期；打开阅读不请求模型。
- 中途删稿后的网络失败、空返回、轮数耗尽和未解决工具错误不发布候选；首次订阅核实后只续跑一次，普通内容核实和跨聊天恢复不请求模型。

### 8.2 已执行的浏览器检查

Playwright 加载实际构建的 Shell 与 WorldApp，仅模拟桥接和新闻数据：

- 320px／390px／桌面，浅色／深色，无横向溢出；320px 配合 150% 放大和长标题。
- 文章焦点、菜单键盘 Enter／Escape、返回滚动。
- 阅读中更新、主动读取新版、阅读中的文章退出当前刊物。
- 保存未确认时禁止刷新，核实后恢复；阅读操作零 World 请求。

截图与临时脚本在 `output/playwright/`。这证明正式组件的浏览器行为，不证明实际 Agent 生成质量。

### 8.3 待验收

- 真实 World Agent 在独立测试聊天生成现代、奇幻、科幻新闻。
- 时间只过几分钟、数日后延续、明确剧情纠正旧背景，及主剧情自然使用背景但角色不全知。
- 真实群聊全程、复杂用户预设及 WebView。

没有真实 Provider 质量证据，不能将首版标为全部验收通过。

## 9. 项目检查与交付

### 9.1 首版后追加：任务与地图素材包

用户确认只增加资料，不建设联动工作流。唯一来源仍是 `world` 分区的已确认版本；订阅和 D4 开关不限制 APP 内取资料。

- `apps/world/context-capability.ts` 与 `module.ts`：提供当前聊天完整 `{overview,news}`，不暴露偏好；空内容或无提供者返回空，读失败报告后省略可选资料。只读返回独立副本，注销后释放提供者，不存缓存或新持久字段。
- `host/prompt-context/maintenance-background.ts`：沿用原人物／世界书／剧情素材组装，补入 `buildWorldDataMessage`；地图、任务独立维护也能读取新闻。已有 World session 时采用它的初始资料，不放第二份。重建地图只排除旧地图投影，不排除世界新闻。
- `apps/tasks/host/context-adapter.ts` 与 `generation/`：刷新委托、寻找应征者复制 capability 提供的已验证完整新闻，任务侧不再重复解析 World 存档规则。新闻是本次请求的固定参考快照，不参与提交前的实时上下文比较；后台新闻更新不丢弃任务结果，剧情、聊天身份和任务自身的变更仍受原有校验约束。
- 资料说明复用 `apps/world/prompt-data.ts` 的同一份预算包装，没有新写一层模型身份、联动命令或收尾。通常完整，超过 16,000 码点时省略正文；地图、任务不会因此获得额外工具或请求，包装也不会要求它们调用不存在的 WorldRead。主剧情 D4 仍为 2,000 码点短投影。
- `world-context.test.js` 覆盖已确认内容／失败恢复／开关独立／注销；真实维护队列的独立与合并运行；任务两个生成请求的安全完整资料及超预算降级；空来源、读取失败与跨聊天隔离。没有额外模型请求。
- `tasks-generation-request.test.js` 覆盖委托板与应征者的在途新闻更新：保留生成结果；同期间剧情改变则仍取消，保持原有边界。

### 9.2 项目检查

最终运行并记录：OS 全量测试、OS lint、改动 JS 的 ESLint、imports、含 vue-tsc 的 OS 构建、`git diff --check`。构建成套更新 Host／Shell／样式与新 World chunk，不只保留旧 hash 删除。

2026-09-06 首轮 Review 修复后结果：

- OS 全量测试 561 项通过，0 失败；包含整轮发布边界、任务参考快照、订阅核实续跑及原有关窗取消／素材包回归。
- OS lint、改动 JS 的 ESLint、imports 通过；最终构建含 vue-tsc 检查通过。
- 首版浏览器证据见 8.2。本轮未修改布局或 D4 宿主适配，未重跑浏览器或真实模型；错误与恢复路径通过真实维护／事务链路的自动测试验证。
- `git diff --check` 通过；dist 共 21 个 JS 文件，本轮检出的 18 个本地产物引用全部存在。新增 World chunk 与 Shell／Host／样式一起保留。
- 隔离浏览器及两个测试服务已关闭。没有修改用户存档，没有提交或推送。

### 9.3 API 失败与保存恢复反馈复查

- 共享维护流程保留安全 Provider 分类，世界、任务与地图采用同一分类：身份验证、权限、请求不兼容、模型／接口不存在、大小限制、限流／额度、超时、服务不可用。仅分类与本地文案进入 UI，不传错误正文、响应体或密钥，不增加自动重试。
- 任务把未开始检查与检查后无需变化分开；文件核实失败但仍有候选时保留核实入口和写入禁用。确认／采用成功后清除对应的旧生成失败，不清除其他 API 错误；失败不能报成功。世界在候选仍待核实时也明确引导再次核实，而不是提示重新生成或读取。
- 最新 OS 全量测试 568 项通过；OS lint、改动测试的 ESLint、imports、含类型检查的构建和 diff 检查通过。真实服务／事务／Controller 测试覆盖未确认→再次核实失败→确认或采用，以及离开页面后在其他入口确认。
- Playwright 检查正式构建：任务核实失败仍可核实、生成禁用、恢复后提示不重复、采用失败不报成功；任务和世界 320px／390px／1100px、深浅主题无横向溢出，打开与导航未发起生成请求。故障数据和桥接被模拟，不代表真实 Provider 故障验收。
- 临时预览和截图位于忽略目录 `output/playwright/`；未调用真实模型、未修改聊天数据，未提交或推送。

### 9.4 存档与投影预算解耦复查

- 领域只校验当前字段与容量；注入预算归 APP 消费方，说明文字不再参与存档加载和保存判定。后台超限省略正文、主剧情按完整段落选取，原始新闻保持不变，没有新增持久态或数据迁移。
- 任务复制 capability 已验证资料，不再重复运行 World 解析。World 的资料标签、首刊条件、读取时机与失败修正说明已对齐实际行为；共享工具说明区分此前成功调用与本次失败批次。
- 新增四项行为回归，覆盖最坏转义内容经过工具／保存／重开、正文补读、完整安全段落预算、失败批次放弃新稿或保留原稿、地图与任务共用降级投影。OS 全量 572 项通过，0 失败；OS lint、改动测试 ESLint、imports、含 vue-tsc 的构建均通过。
- dist 已成套重建，21 个 JS 文件中检出的 18 个本地产物引用均存在；diff 检查通过。本轮未改 UI、宿主注入位置和触发时机，未重跑浏览器或真实 Provider，未修改用户聊天，未提交或推送。

### 9.5 世界 APP 误报聊天切换修复

- 根因：宿主运行时身份以角色列表编号组成，sidecar 身份以角色文件名组成；世界 APP 把两者直接比较，导致单角色聊天打开失败，也会阻止维护会话及背景资料读取。
- `WorldView` 明确区分临时的运行时 `chatIdentity` 与存储 `identityKey`：UI、维护来源与资料消费者比较前者；偏好和新闻提交仍使用后者。读取仅来自当前聊天的 scoped store，没有修改宿主身份规则、存档字段或数据版本。
- 偏好操作在异步预检前捕获存储身份，保留现有页面 guard 和事务 guard；真正切聊天仍拒绝旧操作。没有迁移、清空新闻或增加自动模型请求。
- 回归覆盖两种身份不同的打开／订阅／生成保存／状态推送／重开，以及背景开关、D4 确认快照、任务／地图资料读取和切聊天隔离。测试通过真实服务、维护队列和事务存储链路运行，Provider 与宿主事件由测试提供；不等同于真实浏览器或真实模型验收。
- 验证：OS 全量 573 项通过；OS lint、改动测试 ESLint、imports、含类型检查的完整 OS 构建通过。构建仅 Host 产物发生变化，其他产物保持不变；未提交或推送。

### 9.6 世界窗口视觉与阅读改造

- 范围只包含世界 APP 的页面、局部样式、品牌 SVG、桌面图标及主题色描述。生成、维护、存储、D4 注入、任务／地图资料包均不变，没有新增模型输出字段或持久化实体。
- 替换旧小刊布局：单一「世界」标题、本地远景开篇、两行概况预览和显式展开、同级新闻列表、无重复摘要的正文页。旧刊头、印章、首条放大与编号样式直接移除，不保留旧 UI 分支。
- 菜单点击外部、Escape 与 Tab 离开均可关闭；正文返回仍恢复列表滚动和条目焦点，后台移除当前文章后返回聚焦首页标题。窄屏和 200% 放大时工具栏允许换行，不挤掉操作入口。
- 正式构建的 Shell／WorldApp 经隔离桥接和手写新闻数据完成 41 项浏览器断言：320px／390px／1200px、深浅主题；150% 放大与长标题；200% 工具栏和设置宽度；完整长文、概况展开；阅读中更新／删除、主动读新版、返回位置；菜单键盘／外部点击；未确认与冲突恢复、更新失败和更新中的旧文阅读；减少动态效果；空态订阅及重进。浏览、展开和返回不发起 World 请求，控制台无错误或警告。
- 已查看首页、正文、窄屏放大及桌面截图。脚本和图片位于忽略目录 `output/playwright/`；最终首页为 `world-redesign-final-light.png`／`world-redesign-final-dark.png`。这些结果证明正式组件的布局和交互，不是独立 HTML 样稿，也不冒充真实模型或 WebView 验收。
- OS 全量 700 项测试通过；OS lint、imports、含 vue-tsc 的完整构建和 diff 检查通过，19 个本地 dist chunk 引用均存在。World 新 hash 与 Shell／Host／CSS 成套更新；Host 的变化仅为世界 APP 描述中的主题色。
- 本轮未调用真实模型、未修改用户聊天，未提交或推送。原有真实 Agent 内容质量与宿主待验收项仍见 8.3。

### 9.7 清透阅读版式复验

- 保持原阅读/维护边界，移除昏沉灰绿、大字号和重复栏目标题。开篇为浅蓝地平线，概况在图下独立排版；深色复用同一 SVG 几何，按页面主题采用夜色。桌面图标与局部主题色同步，未改 OS 全局字体/颜色。
- 同级新闻保持正常尺度与留白，640px 以上内容区分两列；未改宿主手机窗尺寸。文章、菜单和空态去掉重复说明，保留费用、失败、核实、订阅和剧情背景信息。
- 正式构建/隔离桥接重新跑通 41 项浏览器断言：320/390/1200px 深浅主题、150%/200% 放大、长文、概况展开、阅读中更新/删除、返回滚动/焦点、失败恢复、空态订阅和重进；另实测 700px 下宽内容区两列。已查看实际截图，阅读与主题切换没有 World 请求。截图留在忽略目录 `output/playwright/world-refined-*.png`。
- OS 714 项、OS lint、imports、含类型检查的三路构建和 diff 检查通过，产物成套更新，20 个本地构建引用均存在。仅使用手写新闻和隔离宿主桥接，不将结果称为真实模型生成质量或用户 WebView 验收；未提交、未推送。
